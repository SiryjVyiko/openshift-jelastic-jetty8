#!/bin/bash

jetty_pid=`ps aux|grep java|grep jetty|grep -v grep|awk '{print $2}'`;

    if [ -n "$jetty_pid" ] 
    then
       kill $jetty_pid && echo "Jetty8 stopped successfully" || echo "Jetty8 stop failed"
    else 
	echo "Jetty8 already stopped";
    fi


